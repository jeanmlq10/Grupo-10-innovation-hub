# Copy this to .env, or pass the same keys as --dart-define / --dart-define-from-file
# when running/building. NEVER commit a real .env — see .gitignore.
#
# flutter run --dart-define-from-file=.env
# flutter build web --dart-define-from-file=.env

# From the Roble console, project settings.
ROBLE_BASE_URL=https://roble-api.test-openlab.uninorte.edu.co
ROBLE_CONTRACT_ID=movil_flutter_dcaabc5f4e

# Name of the "destino de retorno" registered in the Roble console under
# Usuarios > Proveedores. This is a NAME, not a URL — register one per
# environment (e.g. innovation-hub-web-dev -> http://localhost:5001,
# innovation-hub-mobile -> a custom app scheme) and switch this value per
# build. See CODEBASE_INDEX.md / AUTH_DELIVERABLES.md for the full list
# this app expects to exist.
ROBLE_SSO_REDIRECT=innovation-hub-web-dev

# Only required to sign in with Google on iOS (the iOS OAuth client id,
# NOT the web one Roble stores).
ROBLE_GOOGLE_IOS_CLIENT_ID=

# Domain new self-registrations must belong to. Leave empty to allow any.
INSTITUTIONAL_EMAIL_DOMAIN=uninorte.edu.co
